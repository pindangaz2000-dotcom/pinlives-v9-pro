# Versions below are the ones this system was actually run and tested against.

# Web layer
fastapi==0.141.1
uvicorn[standard]==0.52.1
pydantic==2.13.4

# Persistence
SQLAlchemy==2.0.51
# Only needed when DB_TYPE=postgres; harmless otherwise.
psycopg2-binary==2.9.9

# Telegram
Telethon==1.44.0
python-telegram-bot==20.3

# Runtime support
aiohttp==3.14.3
python-dotenv==1.2.2
psutil==7.2.2

# OCR: PP-OCRv4 recognition + detection via onnxruntime (primary OCR backend).
# Tesseract stays as the fallback when this is absent.
rapidocr-onnxruntime==1.4.4
onnxruntime==1.28.0

# Tests
pytest==9.1.1
pytest-asyncio==0.24.0
httpx==0.24.1
